# Benchmarks Workspace

This directory is a local workspace for benchmark datasets, manifests, and reports.

It is intentionally excluded from source control except for this placeholder file.

Typical subdirectories created at runtime:

- `aggregations/`
- `comparisons/`
- `datasets/`
- `manifests/`
- `runs/`
- `trends/`
